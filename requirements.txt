# Только реально импортируемые пакеты (проверено grep по src/, tests/, tools/).
# torch: нужна CUDA-сборка, обычный `pip install -r requirements.txt` поставит
# CPU-only колесо. Сначала отдельно:
#   pip install torch==2.8.0 --index-url https://download.pytorch.org/whl/cu128
# затем уже requirements.txt (torch не переустановится, версия совпадает).
torch==2.8.0
numpy==2.2.6
pyarrow==23.0.0
tokenizers==0.22.2
comet_ml==3.58.6
PyYAML==6.0.3
matplotlib==3.10.8
psutil==7.2.2
